apiVersion: v1
kind: ConfigMap
metadata:
  name: special-config1
data:
  db: mymongodb
  app: myapp
  name: madhu
---
apiVersion: v1
kind: Pod
metadata:
  name: test-pod
spec:
  restartPolicy: Never
  containers:
    - name: test-cont
      image: alpine
      command: [ "/bin/sh", "-c", "echo $(KEY1) $(KEY2)" ]
      env:
        - name: KEY1
          valueFrom:
            configMapKeyRef:
              name: special-config1
              key: db
        - name: KEY2
          valueFrom:
            configMapKeyRef:
              name: special-config1
              key: app
