apiVersion: v1
kind: Pod
metadata:
  name: nginx-liveness-success
  labels:
    app: nginx-liveness-success
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80
    livenessProbe:
      httpGet:
        path: /
        port: 80
      initialDelaySeconds: 10
      periodSeconds: 5
      failureThreshold: 3
